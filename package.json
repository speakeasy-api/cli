{
  "type": "module",
  "name": "@gram-ai/cli-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @gram-ai/functions build && pnpm --filter @gram-ai/create-function build",
    "test": "pnpm --filter @gram-ai/functions test"
  },
  "packageManager": "pnpm@10.30.2",
  "devDependencies": {
    "@changesets/cli": "^2.29.7"
  }
}
